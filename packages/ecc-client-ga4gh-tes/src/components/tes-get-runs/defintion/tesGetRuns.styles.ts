import { css } from "@microsoft/fast-element";

const styles = css`
  .container {
    display: block;
  }

  .search {
  }

  .list {
    padding-bottom: 1rem;
  }

  .pagination {
    display: flex;
    justify-content: center;
    gap: 10px;
    margin: auto;
  }
`;

export default styles;
