{
  "name": "ecc-utils-design",
  "description": "Encapsulation &amp; Utility components to manage the headless yet constitent design accross ecc component",
  "main": "dist/src/index.js",
  "module": "dist/src/index.js",
  "exports": {
    ".": "./dist/src/index.js"
  },
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "author": "",
  "license": "ISC",
  "repository": "",
  "scripts": {
    "analyze": "cem analyze --litelement",
    "build": "tsc && npm run analyze -- --exclude dist",
    "dev": "tsc && concurrently -k -r \"tsc --watch --preserveWatchOutput\" \"wds\"",
    "test": "tsc && wtr --coverage",
    "test:watch": "tsc && concurrently -k -r \"tsc --watch --preserveWatchOutput\" \"wtr --watch\"",
    "storybook": "tsc && npm run analyze -- --exclude dist && concurrently -k -r \"tsc --watch --preserveWatchOutput\" \"wds -c .storybook/server.mjs\"",
    "storybook:build": "tsc && npm run analyze -- --exclude dist && build-storybook",
    "lint": "npx eslint .",
    "lint:fix": "npm run lint -- --fix",
    "prepublish": "tsc && npm run analyze -- --exclude dist"
  },
  "devDependencies": {
    "@custom-elements-manifest/analyzer": "^0.8.4",
    "@open-wc/eslint-config": "^12.0.0",
    "@open-wc/testing": "^3.2.0",
    "@web/dev-server": "^0.3.0",
    "@web/dev-server-storybook": "^1.0.2",
    "@web/test-runner": "^0.17.0",
    "concurrently": "^8.2.1",
    "eslint": "^8.41.0",
    "eslint-config-elixir": "*",
    "eslint-plugin-prettier": "^4.2.1",
    "tslib": "^2.6.2",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@shoelace-style/shoelace": "^2.8.0",
    "lit": "^2.8.0"
  }
}
