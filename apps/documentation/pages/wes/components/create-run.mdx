import { Callout, Tabs } from 'nextra/components';
import ComponentName from '@/components/utils/componentName.tsx';
import ECCClientGa4ghWesCreateRun from '@/components/packages/wes/createRun/createRun';

# Wes Create Run Component

<ComponentName
  tagName='ecc-client-ga4gh-wes-create-run'
  reactTagName='ECCClientGa4ghWesCreateRun'
  version='0.0.0'
  beta
/>

This component is used to create workflow runs using WES API.

## Preview

<ECCClientGa4ghWesCreateRun />

## Imports

<Tabs items={['React', 'HTML/Lit']}>
  <Tabs.Tab>
    ```ts copy
    import ECCClientGa4ghWesCreateRun from '@elixir-cloud/wes/dist/react/create-run/index';
    ```
  
    ### Example
  
    ````ts showLineNumbers {2, 5} copy filename="ECCClientGa4ghWesCreateRun"
    import React from 'react';
    import ECCClientGa4ghWesCreateRun from '@elixir-cloud/wes/dist/react/create-run/index';

    export default function Runs() {
      return <ECCClientGa4ghWesCreateRun />;
    }
    ````

  </Tabs.Tab>
  
  <Tabs.Tab>
    ```js copy
    import "@elixir-cloud/wes/dist/components/create-run/index";
    ```
  
    ### Example
  
    ````html showLineNumbers {6, 8-10} copy filename="ecc-client-ga4gh-wes-create-run"
    <body>
      <div id="demo"></div>
    </div>
    <script type="module">
    import { html, render } from "lit";
    import "@elixir-cloud/wes/dist/components/create-run/index";

    render(
      html`<ecc-client-ga4gh-wes-create-run />`,
      document.querySelector("#demo")
    );

    </script>
    ````

  </Tabs.Tab>
</Tabs>

## Properties

| Property              | Required | Default                                   | Type     | Description |
| --------------------- | -------- | ----------------------------------------- | -------- | ----------- |
| [`baseURL`](#baseURL) | `false`  | `https://protes.rahtiapp.fi/ga4gh/tes/v1` | `String` | Base URL    |

### baseURL

This property is used to give `POST` endpoint to create `Task run`. The provided baseURL is concatenated with `/runs` and is used as an endpoint to create a `POST` request.

<Callout emoji='🌝' type='info'>
  Wanna change it and try, maybe locally deploy
  [proWES](https://github.com/elixir-cloud-aai/proWES)!, to change, pass the baseURL as
  `http://localhost:8090` to the component.
</Callout>

<Callout emoji='🚨' type='error'>
  proWES is still in beta, many of its feature are yet to be stable.
</Callout>

## Parts

<Callout emoji='👩‍🏭'>Hold on! We are working here.</Callout>
