import { Callout, Tabs } from 'nextra/components'
import ComponentName from '@/components/common/ComponentName'
import RunPreview from '@/components/packages/wes/RunPreview'

# EccClientGa4ghWesRun Component

<ComponentName
  tagName='ecc-client-ga4gh-wes-run'
  reactTagName='ECCClientGa4ghWesRun'
  version='2.0.0-alpha.1'
/>

## Overview

The `ecc-client-ga4gh-wes-run` component displays the detailed information and status of a single workflow run.

## Usage

### Basic Run Details

This example shows the details for a specific workflow run ID.

<Tabs items={['Preview', 'React', 'HTML']}>
  <Tabs.Tab>
    <RunPreview />
    <Callout type="info" emoji="ℹ️">
      The preview above uses a fixed `runId`. Since the WES instance is not production ready, this run may not exist.
    </Callout>
  </Tabs.Tab>

  <Tabs.Tab>
    ```tsx showLineNumbers copy filename="run-basic.tsx"
    import React from 'react';
    import { ECCClientGa4ghWesRun } from '@elixir-cloud/wes/react';
    import { RestWesProvider } from '@elixir-cloud/wes/providers';

    export default function RunBasic() {
      const provider = new RestWesProvider(
        'https://weskit.anuragxd.com/ga4gh/wes/v1'
      );

      return (
        <ECCClientGa4ghWesRun 
          provider={provider}
          runId="placeholder-run-id"
        />
      );
    }
    ```
  </Tabs.Tab>
  
  <Tabs.Tab>
    ```html showLineNumbers copy filename="run-basic.html"
    <!DOCTYPE html>
    <html lang="en">
      <head>
        <script type="module" src="https://esm.sh/@elixir-cloud/wes@2.0.0-alpha.1"></script>
      </head>
      <body>
        <ecc-client-ga4gh-wes-run
          run-id="placeholder-run-id"
        >
        </ecc-client-ga4gh-wes-run>

        <script type="module">
          import { RestWesProvider } from 'https://esm.sh/@elixir-cloud/wes@2.0.0-alpha.1/providers';
          
          const provider = new RestWesProvider(
            'https://weskit.anuragxd.com/ga4gh/wes/v1'
          );
          
          document.querySelector('ecc-client-ga4gh-wes-run').provider = provider;
        </script>
      </body>
    </html>
    ```
  </Tabs.Tab>
</Tabs>

## Properties

| Property   | Type                      | Default | Description                                                                    |
|------------|---------------------------|---------|--------------------------------------------------------------------------------|
| `runId`    | `string`                  | `""`    | ID of the workflow run to display details for.                                 |
| `provider` | `WesProvider` | `null`  | Data provider for fetching run data (see [Providers](/docs/wes/providers))  |

## Events

| Event Name             | React Event Name        | Detail Type                    | Description                                    |
|------------------------|------------------------|--------------------------------|------------------------------------------------|
| `ecc-run-log-changed` | `onEccRunLogChanged` | `{ runId: string, runLog: object }` | Fired when the run log data is fetched or updated. |
